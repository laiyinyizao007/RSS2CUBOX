
on:
  rss:
    url:
      - path: https://api.feeddd.org/feeds/611e1fe88fae751e23641327
        subject: apple
      - path: https://sspai.com/feed
        subject: banana
jobs:
  request:
    name: Make a HTTP Request
    runs-on: ubuntu-latest
    steps:
      - name: Make a HTTP Request
        uses: actionsflow/axios@v1
        with:
          url: ${{ secrets.CUBOXAPI }}
          method: POST
          body: |
            {
              "type":"url",
              "content":"${{ on.rss.url.path.outputs.link }}", 
              "title": "${{ on.rss.url.path.outputs.title }}",
              "tags":["${{ on.rss.url.subject.outputs.subject }}"],
              "folder":"RSS"
            }
